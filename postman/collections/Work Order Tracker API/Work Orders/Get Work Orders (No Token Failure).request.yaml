$kind: http-request
url: "{{baseUrl}}/api/work-orders"
method: GET
auth:
  type: noauth
scripts:
  - type: afterResponse
    code: pm.test("returns 401 unauthorized", () => pm.response.to.have.status(401));
    language: text/javascript
order: 6000
