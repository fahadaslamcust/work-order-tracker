$kind: http-request
url: "{{baseUrl}}/api/work-orders/{{createdWorkOrderId}}"
method: GET
scripts:
  - type: afterResponse
    code: pm.test("returns single work order", () => pm.response.to.have.status(200));
    language: text/javascript
order: 3000
