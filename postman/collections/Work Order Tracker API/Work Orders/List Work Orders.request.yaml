$kind: http-request
url: "{{baseUrl}}/api/work-orders"
method: GET
scripts:
  - type: afterResponse
    code: pm.test("returns list of work orders", () => pm.response.to.have.status(200));
    language: text/javascript
order: 1000
