$kind: http-request
url: "{{baseUrl}}/api/work-orders/{{createdWorkOrderId}}"
method: DELETE
scripts:
  - type: afterResponse
    code: pm.test("deletes work order", () => pm.response.to.have.status(204));
    language: text/javascript
order: 5000
