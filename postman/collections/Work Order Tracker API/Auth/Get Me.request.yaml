$kind: http-request
url: "{{baseUrl}}/api/auth/me"
method: GET
scripts:
  - type: afterResponse
    code: pm.test("returns current user", () => pm.response.to.have.status(200));
    language: text/javascript
order: 3000
